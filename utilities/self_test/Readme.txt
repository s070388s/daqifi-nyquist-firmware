Running this batch file will connect to the board via USB and set all DIO ports as output.
Then it will read all AIn channels and display the result.  Then it will read all DIO channels and display the output.
Finally, it will sequence through all DIO channels so any LEDs will light when powered as output.